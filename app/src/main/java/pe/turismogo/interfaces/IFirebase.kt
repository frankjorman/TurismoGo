package pe.turismogo.interfaces

interface IFirebase {
    fun setFirebaseListeners() { /*Here firebase listeners are set*/ }
    fun removeFirebaseListeners() { /*Here firebase listeners are removed*/ }
}